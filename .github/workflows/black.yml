# This workflow will clear inject.sqlite3, backup.sqlite3 and database.sqlite3

name: Format files

on:
  push:
    branches: ['main']

permissions:
  contents: "write"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'
    - name: Format files
      run:
        pip install black --upgrade
        python -m black .
